import style from './header.module.scss'

const Header = () => {
    return(
        <header className={style.header}>
            <h1>Youtube App</h1>
        </header>
    );
}

export default Header;